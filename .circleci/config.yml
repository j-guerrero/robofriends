version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "A first hello"
  dan:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "test#2"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - dan